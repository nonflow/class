commands:
  python:
    sentence:
      - connect Account email="tom@domain.com"
      - list Message from=2024-08-01 to=today
      - create Message sender="user@example.com" content="Hello, world!" subject="Test"
      - read Message id=1
      - delete Message id=1
      - disconnect Account
    alias:
      param:
        from: from_date
        to: to_date
    convert:
      param:
        today: date_converters.current_date
        yesterday: date_converters.previous_date
